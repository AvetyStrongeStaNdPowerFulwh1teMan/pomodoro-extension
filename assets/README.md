Put your notification sound here as:

```text
lofi.mp3
```

The extension will play `assets/lofi.mp3` when a work phase or break phase ends. If the file is missing or the browser blocks it, the extension uses a short generated beep as a fallback.
